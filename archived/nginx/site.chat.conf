server {
	server_name chat.saysimsim.ru;
    listen 80;
    listen [::]:80;
    charset UTF-8;
    ssi on;
    include conf.d/location.certbot;

    location / {
        return 301 https://$host:443$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name chat.saysimsim.ru;
    ssl_certificate /etc/letsencrypt/live/saysimsim.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/saysimsim.ru/privkey.pem;


    location / {
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://saysimsim.ru:8080;
    }

    location /zerva-websocket {
        proxy_pass http://saysimsim.ru:8080/zerva-websocket;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
    }

}
services:
    dnsmasq:
        image: andyshinn/dnsmasq:latest
        container_name: dnsmasq
        restart: unless-stopped
        ports:
            - "5053:53/tcp"
            - "5053:53/udp"
        volumes:
            - ./server_adt/etc/dnsmasq.conf:/etc/dnsmasq.conf:ro
